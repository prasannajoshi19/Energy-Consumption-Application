<div class="home_container">
    <div class="home_header">
        <div class="home_title">
            <div class="wrapper">Energy Consumption Calculator</div>
        </div>
    </div>
    <div class="home_content">
    {{#if formSelected}}
        {{view Energy.FormView}}
    {{else}}
        {{view Energy.ResultView}}
    {{/if}}
    </div>
</div>